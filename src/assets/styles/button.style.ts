import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 10,
        borderRadius: 10,
        marginLeft: 20,
    },
    text: {
        textAlign: 'center',
        fontSize: 19,
        fontWeight: 'bold',
        letterSpacing: 2,
        lineHeight: 40,
    }
});