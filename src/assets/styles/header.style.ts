import { StyleSheet } from "react-native";

export default StyleSheet.create({
    header: {
        padding: 10,
        flexDirection: 'row',
        alignItems: 'center',
    },
    heading: {
        fontWeight: "900",
        fontSize: 50
    },
    subHeader: {
        paddingTop: 10,
        fontSize: 18
    }
})