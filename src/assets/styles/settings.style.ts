import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        marginTop: 20,
        height: '80%',
        alignItems: 'center',
    },
    buttons: {
        flex: 1,
        justifyContent: 'space-evenly'
    }
});