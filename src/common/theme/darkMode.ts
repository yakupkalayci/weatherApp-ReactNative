// Import Type
import {Theme} from './type';

// Import Colors
import {BASE_COLORS} from '../constants/baseColors';

export const darkMode: Theme = {
  backgroundFromColor: 'rgb(0, 0, 0)',
  backgroundToColor: 'rgb(52, 235, 165)',
  primaryText: BASE_COLORS.lightGray,
  secondaryText: BASE_COLORS.darkGray,
  backgroundColor: BASE_COLORS.black,
  timelyCardBackgroundColor: 'rgb(35, 117, 86)',
  footerBgColor: '#121212',
};
