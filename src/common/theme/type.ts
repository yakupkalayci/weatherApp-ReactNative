export type Theme = {
    backgroundFromColor: string;
    backgroundToColor: string;
    primaryText: string;
    secondaryText:string;
    backgroundColor: string;
    timelyCardBackgroundColor: string;
}

export type Themes = 'dark' | 'light';